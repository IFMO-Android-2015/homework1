<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NumberButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">12pt</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@drawable/firstbutton</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="OperationButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">12pt</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@drawable/firstbutton</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="SecondButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">12pt</item>
        <item name="android:textColor">#000000</item>
        <item name="android:background">@drawable/secondbutton</item>
        <item name="android:textStyle">normal</item>
    </style>

</resources>
